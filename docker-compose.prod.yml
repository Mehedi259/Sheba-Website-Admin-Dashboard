version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sheba-admin-dashboard
    restart: always
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
